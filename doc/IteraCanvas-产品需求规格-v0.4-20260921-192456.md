# IteraCanvas（迭绘）产品需求规格

## 文档信息

- 文档版本：v0.4
- 基于版本：v0.3（2026-09-21 19:07:54）
- 修订时间：2026-09-21 19:24:56（Asia/Shanghai）
- 修订说明：确认父任务存在子分支时限制删除，冻结分支删除顺序和 `409 Conflict` 行为。
- 文档状态：需求草案（非需求基线）
- 产品名称：IteraCanvas（迭绘）
- 当前阶段：本地单用户 Demo 阶段 1

> 本文档区分“阶段 1 已确认需求”“建议设计”和“后续阶段未决问题”。阶段 0 决策可作为阶段 1 开发和测试依据；供应商、隐私、完整补丁规则及正式实验等剩余 P0 未关闭，因此本文档仍不是正式产品验收基线。

### 基线生效条件

仅当以下条件全部满足后，文档状态才可改为“需求基线”：

1. 第 17 章十项 P0 待解决问题均有明确结论、责任人和决策记录。
2. 创作规格、规格版本、图片提交、生成轮次、诊断、补丁和分支的数据结构通过评审。
3. 明确新产品与现有提示词 RAG、课程交付要求的保留、复用、改造或退出关系。
4. 确定首个多模态模型供应商、授权与删除边界，以及可执行的图片和调用限制。
5. 对比实验方案和验收标准具备明确阈值，并可由测试人员独立复现。

### 阶段 0 决策摘要

- 旧提示词 RAG、知识库、旧接口、CLI、Streamlit 和三模式实验全部退出，不做兼容。
- 一次外部生成请求是一轮；上传是提交；一轮允许多次提交和多张候选图。
- 已保存规格采用不可变版本；确认版本不可修改；修改与回退均创建新版本。
- 重大规格变化由系统提示分支，最终由用户决定；已接受任务继续创作必须分支。
- Skills 展厅是阶段 5 的独立次级入口，不参与诊断主指标。
- 阶段 1 技术阈值已冻结；真实供应商只能采用更严格限制。
- 父任务存在子分支时禁止删除，必须从叶子分支开始删除。
- 完整工程决策记录、负责人和截止点见 `IteraCanvas-工程设计-v0.5.md` 第 0.2 节和第 4 章。

## 1. 产品定位

IteraCanvas 是一个面向图片创作者的跨平台创作诊断与迭代助手。

它不负责替用户生成图片，而是帮助用户：

1. 从模糊描述中逐步形成可验证的创作需求。
2. 根据用户在外部平台获得的真实生成结果进行诊断。
3. 找出结果与创作目标之间的差距。
4. 输出平台适配或通用的修改补丁。
5. 记录多轮结果，帮助用户以更少生成轮次达到可接受结果。

产品核心指标是：用户达到可接受结果所需的外部生成轮次。

## 2. 产品边界

### 2.1 已确认范围

- 支持诊断所有类型的图片，不限定特定生图平台。
- 用户在外部平台完成图片生成。
- 用户将生成结果上传到 IteraCanvas。
- 文本和图片分析均通过外部多模态 API 完成。
- 允许用户不填写平台、模型、提示词或参数。
- 已知平台与模型时，系统可输出平台适配建议；未知时输出通用建议。
- 用户是诊断结论和最终结果的最终裁决者。
- 保存任务、图片、规格、诊断、补丁和迭代历史。
- 用户可以永久删除任务及其本地数据。

### 2.2 当前明确不做

- 不内置图片生成 API。
- 不替用户自动选择“最佳图片”。
- 不在 Skills 展厅中执行 Skill。
- Demo 阶段不实现多人协作。
- Demo 阶段不实现在线安装、GitHub 自动同步和 Skill 下载。
- Demo 阶段不要求接入 Codex CLI、MCP 或 Plugin。

## 3. 产品模块

### 3.1 跨平台图片诊断助手

负责创作规格整理、图片上传、逐项诊断、诊断复核、修改补丁、多轮比较、任务分支和结果接受。

### 3.2 图片 Skills 展厅

负责在 Web 中展示本地仓库中的图片类 Skills，例如科研图片风格 Skill 等。

当前仅确定两个模块可以共享同一个后端服务；共享技术底座不等于形成统一产品闭环。两个模块是否必须在同一个 Demo 中交付、如何互相导流，以及是否共享同一组成功指标，均属于第 17.5 节的基线阻断决策。

## 4. 核心用户流程

```text
创建创作任务
→ 用户用自由文本描述创作目标，可上传参考图
→ AI 一次性提出当前缺失的问题
→ 用户回答或跳过问题
→ 系统形成可继续修改的创作规格
→ 用户确认当前规格
→ 用户在外部平台生成图片
→ 用户上传一张或多张结果图片
→ 系统逐项诊断
→ 用户确认或纠正诊断
→ 用户选择一个或多个问题进行修复
→ 系统输出修改补丁
→ 用户在外部平台完成下一轮生成
→ 用户上传下一轮结果
→ 系统仅展示本轮与上一轮的差异
→ 重复迭代，直到用户接受结果
```

> 流程图中的“下一轮”指用户完成一次新的外部生成请求，并在系统中明确选择“创建新一轮”。单次上传不增加轮次；分批、重复上传和一次生成返回多图的规则见第 7.1 节和第 17.3 节。

## 5. 创作规格

### 5.1 已确认原则

- 用户通过自由文本描述需求，不要求填写固定大表单。
- 用户可能并不完全了解自己的需求。
- AI 负责从用户描述和参考图中提取创作规格，并对关键缺失项进行追问。
- 追问采用一次性集中提问方式。
- 用户可以跳过问题。
- 创作规格允许不完整，并可以在后续迭代中继续修改。
- 小幅变化保留在原任务中。
- 目的、主体、比例、核心硬约束或评价标准发生重大变化时，系统提示创建任务分支，最终由用户决定。
- 新分支继承最新已确认规格、参考图、当前提示词和参数，不继承原任务的轮次、候选图、诊断、复核和补丁。
- 已接受任务保持只读；继续创作必须创建分支。

### 5.2 建议结构（待确认）

创作规格由“任务上下文”和“可验证要求列表”组成，而不是要求所有固定字段都有值。

#### 任务上下文

- 目标用途
- 投放平台或使用场景
- 目标受众
- 主体、场景和动作
- 风格、构图、色彩和光线
- 输出比例和尺寸
- 图片中的准确文字及可读性要求
- 排除项
- 参考图片及参考用途

#### 可验证要求 `criteria[]`

每条要求建议包含：

| 字段 | 含义 |
| --- | --- |
| `id` | 要求唯一标识 |
| `category` | 主体、构图、风格、文字、颜色、用途等分类 |
| `statement` | 可直接判断的要求内容 |
| `importance` | 硬约束、软偏好或未决定 |
| `source` | 用户明确、AI 推断或系统建议 |
| `status` | 已确认、待确认或未知 |
| `verification_mode` | 视觉、提示词、参数或用户判断 |

只有已确认的硬约束可以被系统判定为明确通过或未通过。

### 5.3 规格版本（阶段 1 已确认）

- 页面中的未保存内容可以编辑；每次点击“保存规格”都创建新的 `draft` 版本，不覆盖已保存内容。
- 用户确认后版本变为 `confirmed`，内容永久不可修改。
- 修改已确认规格时，从它复制生成新的草稿版本。
- 回退历史规格时，复制历史内容生成新草稿版本，不改写历史版本。
- 图片、生成轮次、诊断和补丁必须绑定产生时使用的规格版本；生成轮次只能绑定已确认版本。
- 当前仓库没有需要迁移的旧 IteraCanvas 结构化业务数据。

## 6. 参考图片

### 6.1 已确认规则

- 支持上传参考图片。
- 参考图片可以用于表达风格、构图、角色或产品、颜色等要求。
- 如果用户没有说明参考图用途，系统自动判断并请求用户确认。
- 系统的自动判断不能被当成用户已经确认的事实。

## 7. 图片上传与多结果处理

### 7.1 阶段 1 已确认规则

- 一次外部生成请求形成一个生成轮次，可以返回一张或多张候选图。
- 多图片场景只展示差异，不进行自动排名，也不替用户选择最佳结果。
- 用户可以选择一张图片继续下一轮，也可以根据多张结果自行判断。
- 提示词和生成参数不是强制输入，但系统应推荐用户提供。

上传动作定义为“图片提交”，不等同于生成轮次。一轮允许多次提交；只有用户明确选择“创建新一轮”才增加轮次编号。同轮相同 SHA-256 文件不重复创建候选图；跨轮重复允许但必须警告。候选图在产生诊断前可以修改所属轮次；产生诊断后必须先删除该候选图的诊断链，或者重新上传到正确轮次。

### 7.2 信息完整度对诊断的影响

- 仅有图片：提供视觉诊断和通用修改建议。
- 图片和提示词：可以输出更精确的提示词修改补丁。
- 图片、提示词和参数：可以同时输出提示词和参数修改建议。
- 信息缺失时，系统应明确说明诊断依据和不确定性。

## 8. 诊断结果

### 8.1 已确认诊断范围

- 明确创作要求的满足情况。
- 构图、色彩、光线和视觉层级等审美问题。
- 根据图片用途判断商业表现。
- 与参考图片的对齐情况。
- 与上一轮结果相比的改善和退步。

审美诊断属于建议，不应自动等同于创作规格失败。

### 8.2 建议结构（待确认）

每条诊断结果与一个创作要求或一个独立观察项关联，并包含：

- 通过、未通过、不确定或不适用。
- 图片中的具体证据及位置。
- 问题严重程度。
- 模型置信度。
- 可能原因。
- 是否违反已确认硬约束。
- 用户确认、纠正或否定的结果。

AI 原始诊断和用户纠正结果分别保存，不相互覆盖。

## 9. 修改补丁

### 9.1 当前工作假设（待确认）

- 用户自行选择需要修复的问题。
- 一轮可以同时选择多个问题。
- 诊断和修改界面默认突出展示差异。
- 当多个修改可能冲突，或一次修改多个变量会降低因果判断能力时，系统应提示用户。

“只展示差异”仅是界面展示原则，不代表系统只保存差异。为保证结果可追溯且可直接使用，系统应同时保存基准快照和结构化差异，并提供合并后的完整提示词与完整参数供用户复制。该建议需按第 17.6 节完成评审后才能成为基线规则。

### 9.2 建议结构（待确认）

- 关联的问题 ID。
- 提示词增加、删除或替换内容。
- 参数原值与建议值。
- 局部重绘、重新构图、重新上传参考图等操作建议。
- 下一轮需要保持不变的正确部分。
- 多个修改之间的冲突。
- 修改可能带来的副作用。
- 适用平台和模型；未知时标记为通用补丁。

## 10. 任务、轮次与分支

### 10.1 阶段 1 已确认规则

- 保存完整任务历史。
- 一次新的外部生成请求形成一个生成轮次；上传只是向该轮提交结果图片。
- 用户可以在任意轮次修改创作规格。
- 重大规格变化由系统提示创建分支，最终由用户决定。
- 分支继承最新已确认规格、参考图、当前提示词和参数，不继承历史轮次和结果。
- 用户明确接受结果后任务完成并转为只读；继续创作必须创建分支。
- 父任务存在子分支时禁止删除；系统返回阻塞分支，用户必须先从叶子分支开始删除。删除子分支不影响父任务。

系统不根据上传动作自动推断新的外部生成行为；页面始终让用户选择“追加到当前轮”或“创建新一轮”。

### 10.2 建议状态模型（待确认）

任务状态：

```text
草稿 → 进行中 → 已接受
```

轮次状态：

```text
等待图片 → 诊断中 → 等待用户复核 → 补丁已生成 → 已结束
```

外部诊断失败时保留当前轮次，允许用户重试，不推进任务状态。

## 11. 本地数据与隐私

### 11.1 已确认规则

- Demo 为本地运行、本人可见的单用户版本。
- 任务数据和图片保存在本地。
- 外部 API 密钥保存在本地 `.env` 文件中。
- 图片发送到第三方模型前必须向用户说明，并获得明确授权。
- 用户可以永久删除一个任务的全部本地数据。
- “永久删除”仅指 IteraCanvas 可控制的本地数据；不得暗示第三方供应商会同步删除其已接收的数据。

### 11.2 建议实现规则（待确认）

- SQLite 保存结构化数据。
- 本地文件目录保存原图和处理后的副本。
- 使用任务 ID 和轮次 ID 隔离文件目录。
- 发送外部 API 前生成去除 EXIF 信息的副本，原图保持不变。
- 删除没有子分支的任务时，级联删除该任务自身的数据库记录、原图和中间文件；存在子分支时拒绝删除且不得产生副作用。
- 支持 PNG、JPEG 和 WebP，具体文件大小和尺寸限制在选定模型后确定。
- 授权记录包含供应商、模型、时间和授权范围。
- 授权界面应披露第三方的用途、保留期限和删除能力；若供应商未提供远程删除能力，必须明确告知用户。
- 用户拒绝或撤回授权后，系统不得继续发起新的外部调用，但仍允许查看、导出或删除已保存的本地数据。

## 12. 外部多模态 API

### 12.1 已确认规则

- 图片与文本分析均通过外部 API 完成。
- 具体供应商和模型尚未确定。
- Demo 不需要同时支持多个供应商。
- 供应商未确定前，可以使用模拟结果开发核心业务流程。

### 12.2 建议失败处理（待确认）

- 模型输出必须通过结构化字段校验。
- 超时、限流和服务端错误只进行有限次数重试。
- 结构化输出无效时，诊断标记失败并允许重试。
- 失败不能生成半完整诊断，也不能破坏已有任务数据。
- 更换图片接收方或供应商时重新获得用户授权。

## 13. Skills 展厅

Skills 展厅是独立次级入口，不参与图片诊断主流程或主指标，不执行或安装 Skill。阶段 1 至阶段 4 不依赖该模块，阶段 5 单独实现。

### 13.1 已确认规则

- Skills 在 Demo 阶段存放于本地仓库。
- Skills 由管理员手动发布。
- 发布前需要进行校验。
- Web 展示 Skill 的说明、案例和完整文本。
- 用户可以复制 Skill 文本并手动粘贴使用。
- Web 不执行 Skill。
- Demo 不提供下载、GitHub 自动同步和在线安装。
- 首批 Skill 清单已确定为第 13.3 节所列 8 项；进入本地采集区不等于取得公开发布或商业使用授权。

### 13.2 建议校验规则（待确认）

禁止发布的错误：

- 缺少 `SKILL.md`。
- 元数据结构无法解析。
- Skill 名称重复。
- 引用文件不存在。
- 路径越界或引用本地仓库之外的文件。

可提示的完整性问题：

- 缺少示例。
- 缺少效果说明。
- 缺少安装或来源元数据。

完整性问题是否允许管理员强制发布仍待确认。

上述“禁止发布”规则适用于声称可直接使用或可安装的完整 Skill 包。本轮按产品要求采集的是 `content_only` 核心正文快照，只用于本机研究、授权评估和展厅数据结构开发；快照可以缺少正文引用的 `references/`、`scripts/` 或 `assets/`，但此时必须标记为“非完整包”，不得进入可发布状态，也不得向用户提供安装入口。

### 13.3 首批 Skills 展厅清单

以下来源已在 2026-09-20 逐项核对，并以提交哈希锁定版本。核心正文保存在 `data/skills-showcase/<id>/SKILL.md`，来源和 SHA-256 见 `data/skills-showcase/manifest.json`。

| ID / 展厅标题 | 上游来源 | 授权与发布结论 |
| --- | --- | --- |
| `heytea-doodle-poster` / 实物＋微型工人涂鸦 | `Hchen1218/heytea-style` | MIT；保留版权与许可声明后可发布。不得暗示为喜茶官方工具。 |
| `scenes-gathered-zine-v1-3` / 场景拾集 Zine | `Zeejay0/gathered-scenes-zine-skill` 的 `skills/scenes-gathered-zine-v1-3/` | 仅个人、非商业使用；发布前需按实际主体和用途复核，组织、客户和商业场景需另行书面授权。 |
| `tait-crt-interface-skill` / 复古 CRT 电脑界面 | `TaiT-tt/tait-crt-interface-skill` | 当前锁定提交未发现许可证文件；仅作本地评估，取得授权前禁止在展厅分发完整正文。 |
| `photo-abstract-editorial` / 照片抽象编辑海报 | `ZzzLc0405/photo-abstract-editorial` | 仅个人、教育、研究与非商业使用；公开或商业发布前需确认授权范围并保留来源。 |
| `yarn-rug-reference` / 毛线地毯效果 | `rlx-better/yarn-rug-reference` | MIT；保留版权与许可声明后可发布。 |
| `travel-memory-sticker-card` / 旅行记忆贴纸卡 | `carolinaaafy/travel-memory-sticker-card` | 仅个人、非商业使用，且明确禁止再分发或作为其他应用、平台、仓库的内置功能；取得书面授权前不得进入展厅发布库。 |
| `gc-minimal-zine-poster-v0-3` / GC 极简杂志海报 | `LiamGvchi/gc-minimal-zine-poster` | MIT；保留版权与许可声明后可发布。 |
| `ip-illustration-for-yourself` / 个人 IP 萌粒风 | `EverettFish/ip_illustration_for_yourself` | 当前锁定提交未发现许可证文件；仅作本地评估，取得授权前禁止在展厅分发完整正文。正文 frontmatter 的实际名称是 `ip-illustration-character-system`，展厅需把仓库别名与调用名分字段保存。 |

### 13.4 本地采集与发布状态

- 本地采集清单以 `data/skills-showcase/manifest.json` 为唯一事实来源，至少记录展厅 ID、frontmatter 名称、来源仓库、来源路径、提交哈希、正文哈希、许可类型和发布状态。
- 第三方核心正文和上游许可证副本默认由 `.gitignore` 排除，避免把本地下载误提交为产品分发内容；清单和本说明可以进入版本控制。
- `ready_with_license_notice` 只表示许可证允许按条件再分发，不表示内容已通过完整性、安全性或品牌合规审核。
- `restricted_review` 只允许本地评估；发布前必须由产品负责人确认使用主体、是否商业化、展示范围和署名方式。
- `blocked_no_license` 与 `blocked_redistribution` 不得发布。管理员强制发布不能绕过许可证限制。
- 从本地采集区复制正文到发布库属于独立发布动作，必须记录操作者、时间、目标版本、授权依据和校验结果。

## 14. Codex 集成规划

### 14.1 已确认方向

- Codex CLI 是附加入口，不是 Demo 主界面。
- 最终可以将核心工作流封装为 Codex Skill。
- Web 与 Codex Skill 共享同一个后端服务。
- 后续可以通过 MCP 暴露创建任务、提交轮次、诊断、生成补丁、比较和接受结果等操作。
- 更长期可以封装为 Plugin。

## 15. Demo 对比实验

### 15.1 已确认目标

- 对比人工修改流程与 IteraCanvas 辅助流程。
- 先使用固定、可重复的创作案例。
- 后续条件允许时再加入真实创作者。
- 核心指标是达到用户可接受结果所需的生成轮次。

### 15.2 计量规则

- 计量候选规则为：一次外部平台生成请求计为一轮，无论该次返回一张还是多张候选图；上传次数不计为轮次。
- 只有用户明确点击“接受结果”才算任务完成。
- 两组实验使用相同需求、参考图、模型版本和初始参数。
- 辅助指标包括耗时、硬约束通过率、修改建议采用率和问题回退数量。
- 实验最大轮次暂按 6 轮执行；固定案例数量、案例类型、单任务成本上限、实验顺序和真实参与人数在阶段 6 开始前确定。
- 为降低学习效应和顺序偏差，人工流程与辅助流程的先后顺序应随机化或采用平衡顺序；同一参与者是否同时体验两组也需在实验前固定。

### 15.3 阶段 1 已确认技术阈值

| 配置 | 决策值 |
| --- | --- |
| 支持图片格式 | PNG、JPEG、WebP |
| 单文件大小 | 10 MiB |
| 最大图片像素 | 40,000,000 |
| 每轮最多图片 | 8 |
| 每任务最多图片 | 50 |
| 每任务存储上限 | 500 MB |
| 模型调用超时 | 90 秒 |
| 最大重试 | 2 次 |
| JSON 结构修复 | 1 次 |
| 实验最大轮次 | 6 |
| 前端轮询间隔 | 1.5 秒 |

接入真实供应商时，只允许采用更严格的限制。

## 16. 非功能要求

- 本地 Demo 中的任务和图片默认仅本人可见。
- 模型调用失败不能造成已保存历史丢失。
- 所有 AI 推断应标注来源和置信度。
- 系统不得把未确认的 AI 推断表述为用户要求。
- 用户纠正诊断后，后续补丁使用纠正后的结论。
- 关键状态变化和外部 API 调用应具有可追踪记录。

## 17. 待解决问题（需求基线阻断项）

以下十项均为 P0。每一项都必须形成可追溯的决策记录，并同步修改正文、数据结构、原型和验收用例；仅在会议中口头达成一致不视为关闭。

| 编号 | 状态 | 待解决问题 | 主要影响 | 关闭产物 |
| --- | --- | --- | --- | --- |
| P0-01 | 未关闭 | 文档基线状态不成立 | 全部研发与验收活动 | 基线评审记录、冻结范围和版本号 |
| P0-02 | 已关闭 | 新产品与现有 RAG 系统关系不明确 | 产品范围、课程交付、迁移成本 | 工程设计 v0.5 的退出决策和删除清单 |
| P0-03 | 已关闭 | “生成轮次”定义冲突 | 数据模型、指标、计费与实验 | 阶段 0 决策 D0-01、D0-02 |
| P0-04 | 已关闭 | 创作规格缺少版本管理 | 历史解释、诊断追溯、分支 | 阶段 0 决策 D0-03、D0-04 |
| P0-05 | 已关闭 | 诊断助手与 Skills 展厅缺少统一闭环 | Demo 范围、导航和成功指标 | 阶段 0 决策 D0-05 |
| P0-06 | 未关闭 | “只展示差异”规则不完整 | 存储、补丁、复制使用体验 | 快照/差异策略和合并输出规则 |
| P0-07 | 未关闭 | 确认、跳过与 AI 推断规则不明确 | 诊断可信度、用户控制权 | 状态转换表和纠正规则 |
| P0-08 | 未关闭 | 隐私、授权与删除边界不完整 | 合规、外部调用、用户信任 | 授权矩阵、删除说明和供应商政策 |
| P0-09 | 未关闭 | 指标和对比实验缺乏可靠计量 | 产品结论与课程实验有效性 | 预注册式实验方案和统计口径 |
| P0-10 | 未关闭 | 缺少可直接测试的验收标准 | 测试、上线判断和回归 | 带阈值的验收用例集 |

### 17.1 P0-01 文档基线状态不成立

**现状**：核心业务规则、数据结构、模型供应商和实验标准均未冻结，将当前文档称为“需求基线”会造成研发和测试把工作假设误当作确定要求。

**建议决策**：当前版本维持“需求草案（非需求基线）”。第 17 章全部 P0 项关闭、核心数据结构通过评审且验收阈值补齐后，再发布新的基线版本；不得直接覆盖本草案。

**关闭标准**：

- 为每个 P0 项记录结论、决策日期、决策人和受影响章节。
- 给出本期范围、明确不做、外部依赖和变更控制方式。
- 发布新版本号并保留本草案，评审人签字或通过等价的电子审批。

### 17.2 P0-02 新产品与现有 RAG 系统关系不明确

**状态**：已关闭。

**决策**：旧提示词 RAG、知识库、向量数据库、三模式实验、CLI、Streamlit 页面及旧 API 全部退出；IteraCanvas 不提供兼容入口，也不复用旧领域模型。历史架构附件只作参考，不构成运行时依赖。

**记录**：2026-09-21 由项目负责人确认；源码和旧依赖已按工程设计 v0.5 第 16.1 节清理。

### 17.3 P0-03 “生成轮次”的定义存在冲突

**状态**：已关闭。

**确认术语**：

- **生成轮次 `generation_round`**：用户在外部平台发起的一次生成请求。
- **图片提交 `submission`**：用户向某个生成轮次上传一次或多次文件的动作。
- **候选图 `candidate`**：一次生成请求返回的单张图片。

**确认规则**：一轮可含多张候选图和多次图片提交；只有用户明确选择“创建新一轮”才增加轮次编号。同轮同 SHA-256 文件不重复创建候选图；跨轮重复允许但必须警告。候选图在产生诊断前可以修改所属轮次；产生诊断后必须先删除该候选图的诊断链，或者重新上传到正确轮次。

**记录**：2026-09-21 由项目负责人确认；阶段 1 必须覆盖分批上传、同轮重复、跨轮重复和归属纠正测试。

### 17.4 P0-04 创作规格缺少版本管理规则

**状态**：已关闭。

**确认规则**：每次保存创建新的 `draft` 规格版本；确认后内容不可修改；修改和回退均通过复制创建新版本。每个生成轮次、候选图诊断和补丁绑定产生时采用的规格版本。重大变化由系统提示创建分支，最终由用户决定。分支继承最新确认规格、参考图、提示词和参数，不继承轮次、候选图、诊断、复核和补丁。已接受任务继续创作必须创建分支。父任务存在子分支时禁止删除，必须从叶子分支开始删除。

**迁移结论**：当前仓库没有旧 IteraCanvas 结构化业务数据，无需业务数据迁移。

**记录**：2026-09-21 由项目负责人确认。

### 17.5 P0-05 诊断助手与 Skills 展厅缺少统一产品闭环

**状态**：已关闭。

**决策**：诊断助手是主产品闭环；Skills 展厅是独立次级入口。两个模块可以位于同一仓库和同一 Demo，但不共享成功指标，不要求跨模块传递数据。移除展厅不影响诊断助手核心价值。展厅不执行或安装 Skill，阶段 1 至阶段 4 不依赖它，阶段 5 单独交付。

**记录**：2026-09-21 由项目负责人确认。

### 17.6 P0-06 “只展示差异”的规则不完整

**现状**：“只展示差异”没有区分界面展示与持久化策略，也没有说明用户如何获得可直接粘贴到外部平台的完整提示词和参数。

**建议规则**：界面默认突出显示新增、删除和替换项；系统同时保存输入基准快照、结构化差异和合并后的结果快照。每个补丁必须关联基准提示词、基准参数和规格版本，并提供“一键复制完整提示词”“复制完整参数”和“查看差异”。合并冲突必须显式提示，不得静默覆盖用户内容。

**关闭标准**：明确快照与差异的唯一事实来源、合并顺序、空值处理、参数删除语义、冲突处理和历史重放方式，并通过多补丁合并与旧版本回放测试。

### 17.7 P0-07 用户确认、跳过问题和 AI 推断之间的规则不明确

**现状**：尚未定义用户确认规格是否会连带确认 AI 推断项、跳过项能否参与诊断，以及用户纠正诊断后如何影响补丁和后续轮次。

**建议规则**：

- “确认规格”只确认界面中明确列出且由用户勾选或编辑确认的内容，不自动把所有 AI 推断转为用户事实。
- AI 推断保持 `待确认` 和 `AI 推断` 来源；可作为明确标注的软建议，但不得用于硬约束通过/失败判断。
- 用户跳过的问题记为 `未知/已跳过`，不参与通过率分母，不得造成任务失败；如参与建议生成，必须说明其假设性质。
- 用户可对诊断执行确认、纠正、不适用或无法判断；后续补丁和比较使用用户纠正后的有效结论，同时保留 AI 原始结果和审计记录。

**关闭标准**：产出 `source × status × importance` 状态转换表，覆盖批量确认、取消确认、跳过后补填、纠正诊断和撤销纠正。

### 17.8 P0-08 隐私、授权与删除边界不完整

**现状**：本地永久删除不代表第三方同步删除。授权范围、授权失效、拒绝授权、调用日志清理以及供应商数据保留策略均未完整定义。

**建议规则**：授权至少绑定任务、供应商、模型、数据类型、用途、时间和政策版本；更换供应商、用途扩大、政策实质变化或授权撤回后再次调用时必须重新授权。拒绝授权时禁止上传到第三方，但允许用户继续使用不依赖外部调用的本地功能。删除任务时删除本地结构化数据、原图、衍生图、缓存和可关联调用日志；第三方删除仅在供应商提供能力时发起，并分别展示本地删除结果与第三方处理状态。

**关闭标准**：选定供应商后记录其训练使用、保留期限、区域、日志、删除接口和失败处理政策；授权文案不得承诺产品无法控制的第三方删除结果。

### 17.9 P0-09 核心指标和对比实验缺乏可靠计量方法

**现状**：“可接受结果”依赖主观点击，一轮候选图数量不受控也可能提高偶然命中概率；最大轮次、成本、耗时和实验顺序尚未固定。

**建议计量框架**：

- 主指标：在固定外部生成请求定义下，达到预先声明的任务验收门槛所需生成轮次。
- 完成条件：用户点击接受，且预先定义的硬约束通过；两者分开记录，不以单一主观点击替代。
- 控制变量：相同案例、模型及版本、初始提示词/参数、每轮候选图数量、最大轮次、单任务预算和超时规则。
- 辅助指标：总耗时、模型与平台成本、硬约束通过率、建议采用率、回退数和失败调用数。
- 顺序控制：随机分组或平衡两种流程的先后次序；正式实验前固定案例和分析方法，不根据结果临时改口径。

**关闭标准**：实验方案明确样本量、案例分层、每轮候选图上限、最大轮次、预算、计时起止点、退出规则、顺序分配和异常数据处理，并用至少一轮预实验验证记录方式可执行。

### 17.10 P0-10 缺少可直接测试的验收标准

**现状**：图片限制、处理时限、失败重试、异常流程、分支、删除、规格版本和诊断纠正均缺少可验证的输入、动作和预期结果。

**最低验收用例集**：

| 用例 ID | 场景 | 最低可验证结果 |
| --- | --- | --- |
| AC-IMG-01 | 上传受支持和不受支持的图片 | 合法文件成功入库；非法格式、超大小或超尺寸文件被明确拒绝，且不创建错误轮次 |
| AC-RND-01 | 一轮多图、分批上传和重复上传 | 图片归属同一指定轮次；重复文件按既定规则处理；轮次计数不因上传次数增加 |
| AC-API-01 | 模型超时、限流、服务错误或结构无效 | 在明确时限和重试次数内结束；失败可重试、无半成品、已有历史不受损 |
| AC-AUTH-01 | 拒绝、撤回或需要重新授权 | 不发生未授权外部调用；本地可用功能仍可访问；重新授权触发条件正确 |
| AC-SPEC-01 | 修改规格后查看旧轮次 | 新建规格版本；旧图片、诊断和补丁仍绑定原版本，不被新规格重写 |
| AC-BRANCH-01 | 重大规格变化并创建分支 | 分支继承规则正确，原任务不变，父子关系可追溯 |
| AC-DIAG-01 | 用户纠正、否定或撤销诊断 | 原始诊断保留；有效结论和后续补丁按用户最新决定更新 |
| AC-PATCH-01 | 生成并应用一个或多个差异补丁 | 可查看差异并复制合并后的完整提示词和参数；冲突不会被静默覆盖 |
| AC-DEL-01 | 删除父任务或含多轮数据的叶子任务 | 父任务存在子分支时返回冲突且无副作用；叶子任务的受控本地数据和关联日志按规则删除；第三方状态单独说明且不误报 |
| AC-RECOVER-01 | 处理中刷新、关闭或重复提交 | 按既定幂等规则恢复，不重复创建轮次、诊断或计费调用 |

**关闭标准**：为所有用例补齐确定数值，包括支持格式、单文件大小、图片尺寸、每轮图片数、任务容量、处理时限、重试次数和最大轮次；使用 Given/When/Then 编写并由测试人员在无口头解释的情况下独立执行通过。

### 17.11 其他非阻断待确认项

以下问题仍需确认，但不替代上述十项 P0 决策：

1. 创作规格 `criteria[]` 是否确定为诊断和补丁的核心关联方式。
2. Demo 首个多模态 API 的具体供应商和模型版本。
3. Skills 完整性校验失败后是否允许管理员强制发布。
4. 首批 8 个图片 Skills 的公开发布授权复核（清单与本地正文采集已完成）。

## 18. 当前开发顺序建议

1. 阶段 0 已完成：P0-02、P0-03、P0-04 和 P0-05 已关闭，阶段 1 规则与阈值已冻结。
2. 实现任务、规格版本、生成轮次、图片提交、诊断、补丁、分支和本地存储。
3. 在阶段 2 前关闭 P0-06 和 P0-07，冻结补丁合并与诊断复核规则。
4. 使用模拟模型跑通完整诊断迭代闭环，并执行 P0-10 的非供应商相关用例。
5. 在阶段 4 前关闭 P0-08 并确定首个多模态供应商，再实现真实外部调用。
6. 接入一个外部多模态 API，补齐图片限制、时限、重试和授权测试。
7. 完成诊断 Web 界面；阶段 5 单独交付 Skills 展厅。
8. 阶段 6 前关闭 P0-09，完成预实验后再开展正式对比实验。
9. 全部 P0 关闭并完成评审后关闭 P0-01，发布正式需求基线。
10. 核心流程稳定后，再增加 Codex Skill、MCP、协作和多供应商能力。
