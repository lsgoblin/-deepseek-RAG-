
# 基于 DeepSeek 的 RAG 信息检索系统

## 可执行项目需求说明书（AI-Friendly Markdown Spec）

> 项目类型：课程项目 / 可实现型软件系统
> 项目目标：实现一个基于 DeepSeek 大语言模型的 RAG（Retrieval-Augmented Generation）信息检索问答系统，支持知识库构建、向量检索、答案生成、结果展示与基础部署。
> 参考依据：课程作业说明文档 

---

## 1. 项目概述

### 1.1 项目名称

**Lab 2：基于 DeepSeek 的 RAG 信息检索系统**

### 1.2 项目目标

构建一个可运行的 RAG 问答系统，系统能够：

1. 导入多篇外部文档并构建知识库；
2. 对文档进行切分、向量化和存储；
3. 根据用户问题进行相似度检索；
4. 将检索结果作为上下文注入 DeepSeek 模型；
5. 输出带来源依据的回答；
6. 提供基础交互界面；
7. 支持课程作业要求中的对比实验与文档提交。

### 1.3 核心能力

* **检索（Retrieval）**
* **增强（Augmentation）**
* **生成（Generation）**
* **可溯源问答**
* **课程实验对比**

---

## 2. 项目范围

## 2.1 必做范围

系统必须包含以下模块：

* 文档加载模块
* 文本切分模块
* 向量化模块
* 向量数据库存储模块
* 相似度检索模块
* Prompt 构造模块
* DeepSeek 调用模块
* 结果展示模块
* 对比实验模块
* 基础交互界面模块

## 2.2 可选扩展范围

可选实现以下增强功能之一或多个：

* 多格式文件上传并动态更新知识库
* 多轮对话历史记忆
* 混合检索（BM25 + 向量检索）
* 缓存机制
* 自动评估脚本

---

## 3. 用户角色与使用场景

## 3.1 用户角色

* **普通用户**：输入问题，查看检索结果和最终答案
* **开发者/助教/教师**：运行系统、验证功能、检查作业完成度

## 3.2 典型使用流程

1. 用户准备 3~5 篇知识文档；
2. 系统加载文档并建立知识库；
3. 用户输入问题；
4. 系统执行检索；
5. 系统将检索结果与问题拼接成 Prompt；
6. 调用 DeepSeek 生成回答；
7. 输出答案和来源；
8. 用户可选择查看对比模式结果。

---

## 4. 功能需求

## 4.1 FR-01 文档导入

### 描述

系统应支持导入知识文档，用于构建知识库。

### 输入

* 本地文件路径或指定知识库目录

### 支持格式

* 必须支持：`txt` / `md` / `pdf` / `docx`
* 可选支持：更多格式通过 `unstructured` 扩展

### 验收标准

* 至少成功加载 3~5 篇文档
* 能提取出纯文本内容
* 对异常文件有错误提示，不导致主流程崩溃

---

## 4.2 FR-02 文本切分

### 描述

系统应将长文档切分为适合向量化和检索的文本块。

### 规则

* 建议 chunk size：`300~500` 字符
* 建议 overlap：`50~100` 字符

### 输出

每个 chunk 必须至少包含：

* `chunk_id`
* `source_doc`
* `chunk_text`
* `chunk_index`

### 验收标准

* 所有导入文档都完成切分
* 单个 chunk 不为空
* chunk 保留来源文档信息

---

## 4.3 FR-03 向量化

### 描述

系统应使用 embedding 模型将文本块转换为向量表示。

### 推荐模型

* 中文优先：`BAAI/bge-small-zh`
* 英文优先：`sentence-transformers/all-MiniLM-L6-v2`

### 输入

* 文本 chunk 列表

### 输出

* 对应向量数组
* 维度与向量数据库兼容

### 验收标准

* 所有 chunk 均成功生成 embedding
* 向量数量与 chunk 数量一致
* 向量维度一致

---

## 4.4 FR-04 向量库存储

### 描述

系统应将 chunk 向量和原始文本存入向量数据库。

### 推荐实现

* 优先：`Chroma`
* 备选：`FAISS`

### 存储内容

* `id`
* `embedding`
* `document`
* `metadata`

### metadata 字段建议

```json
{
  "source": "doc1.pdf",
  "chunk_index": 3,
  "category": "DeepSeek",
  "created_at": "2026-04-21"
}
```

### 验收标准

* 向量数据库可成功创建
* 数据持久化可复用
* 重启后可重新加载索引

---

## 4.5 FR-05 问题检索

### 描述

系统应根据用户问题在向量数据库中检索最相关的文本块。

### 输入

* 用户问题字符串
* top-k 参数，默认 `k=3`

### 输出

* top-k 个最相似文本块
* 每个文本块附带来源信息和相似度分数（可选）

### 验收标准

* 用户每次提问均触发检索
* 检索结果数量等于或小于 k
* 检索结果来源可追踪

---

## 4.6 FR-06 Prompt 构造

### 描述

系统应将检索结果拼接到统一 Prompt 模板中，提交给 DeepSeek。

### 推荐 Prompt 模板

```text
请根据以下【参考内容】回答用户的问题。如果参考内容中没有相关信息，请直接说“根据现有资料无法回答”，不要编造信息。

【参考内容】
{retrieved_chunks}

用户问题：{question}

请给出准确、简洁的回答，并说明信息来源。
```

### 验收标准

* Prompt 中必须包含检索内容
* Prompt 中必须包含用户原始问题
* 对缺失信息场景有明确约束，避免幻觉

---

## 4.7 FR-07 LLM 生成回答

### 描述

系统应调用 DeepSeek API 或本地模型生成最终答案。

### 输入

* 构造后的 Prompt

### 输出

* 最终答案文本
* 可选输出：token 用量、响应时间

### 验收标准

* 能正常调用 DeepSeek
* 输出答案非空
* 答案与检索结果相关
* 答案包含来源说明或来源引用

---

## 4.8 FR-08 对比实验

### 描述

系统必须支持以下三种回答模式对比：

1. **仅 LLM**：直接向模型提问，不提供检索内容
2. **仅检索**：仅返回最相关文档片段
3. **RAG**：检索 + 生成完整流程

### 输出要求

对同一个问题输出三组结果，并允许截图或记录。

### 验收标准

* 至少完成 3 个不同问题的对比
* 输出结果可用于实验报告撰写
* 能体现 RAG 在准确性与可溯源性方面的优势

---

## 4.9 FR-09 基础交互界面

### 描述

系统应提供简单可用的交互入口。

### 最低要求

* 命令行界面（CLI）

### 推荐加分

* Gradio 或 Streamlit Web 界面

### 界面必须展示

* 用户输入问题
* 检索到的相关片段 / 来源
* 最终生成答案

### 验收标准

* 用户可多次连续提问
* 每次提问均可看到检索结果与答案
* 出错时有提示信息

---

## 5. 非功能需求

## 5.1 NFR-01 可复现性

项目必须支持从零环境复现，至少提供：

* `requirements.txt`
* `README.md`
* 启动命令
* API Key 配置说明

## 5.2 NFR-02 可维护性

* 代码按模块拆分
* 关键函数添加注释
* 文件命名统一规范

## 5.3 NFR-03 可扩展性

* 预留 embedding 模型切换接口
* 预留向量数据库切换接口
* 预留多格式文档扩展接口

## 5.4 NFR-04 稳定性

* 文件读取失败不应导致整个系统退出
* 检索为空时必须有兜底响应
* API 调用失败时应有重试或报错提示

## 5.5 NFR-05 课程交付适配

项目结构应方便直接打包提交课程作业。

---

## 6. 技术选型要求

## 6.1 推荐技术栈

| 模块        | 推荐方案                                    | 说明          |
| --------- | --------------------------------------- | ----------- |
| LLM       | DeepSeek API / 本地 DeepSeek 模型           | 用于生成        |
| Embedding | BAAI/bge-small-zh                       | 中文优先        |
| 向量库       | Chroma                                  | 轻量易上手       |
| 文档解析      | pypdf / python-docx / LangChain loaders | 解析 PDF/Word |
| Web 界面    | Gradio / Streamlit                      | 可选加分        |
| 开发语言      | Python 3.10+                            | 主实现语言       |

## 6.2 依赖要求

```txt
chromadb>=0.4.0
faiss-cpu>=1.7.4
sentence-transformers>=2.2.0
langchain>=0.1.0
pypdf>=3.0.0
python-docx>=0.8.11
unstructured>=0.10.0
gradio>=4.0.0
streamlit>=1.30.0
python-dotenv>=1.0.0
requests>=2.31.0
```

---

## 7. 系统架构

## 7.1 逻辑流程

```text
文档输入
   ↓
文本提取
   ↓
文本切分
   ↓
Embedding 向量化
   ↓
向量库存储
   ↓
用户提问
   ↓
问题向量化
   ↓
Top-K 检索
   ↓
Prompt 构造
   ↓
DeepSeek 生成
   ↓
答案 + 来源展示
```

## 7.2 模块划分建议

```text
project/
├─ data/                     # 原始知识文档
├─ vector_store/             # 向量库持久化目录
├─ src/
│  ├─ loaders.py            # 文档加载
│  ├─ splitter.py           # 文本切分
│  ├─ embedder.py           # 向量化
│  ├─ vectordb.py           # 向量数据库读写
│  ├─ retriever.py          # 检索
│  ├─ prompt_builder.py     # Prompt 构造
│  ├─ llm.py                # DeepSeek 调用
│  ├─ rag_pipeline.py       # 主流程
│  ├─ compare.py            # 三种模式对比
│  └─ ui.py                 # CLI / Web 界面
├─ notebooks/
│  └─ demo.ipynb
├─ outputs/
│  ├─ screenshots/
│  └─ experiment_results/
├─ requirements.txt
├─ .env.example
├─ README.md
└─ report.pdf
```

---

## 8. AI 执行指令（面向自动编码代理）

## 8.1 目标

请按以下顺序实现项目，不要跳步：

1. 创建项目目录结构；
2. 完成依赖文件；
3. 实现文档加载；
4. 实现文本切分；
5. 实现 embedding；
6. 实现向量库存储；
7. 实现检索；
8. 实现 Prompt 构造；
9. 实现 DeepSeek 调用；
10. 串联完整 RAG pipeline；
11. 增加 CLI；
12. 增加对比实验模块；
13. 可选增加 Gradio/Streamlit UI；
14. 编写 README；
15. 输出可复现实验结果。

## 8.2 编码约束

* 所有模块都要可独立导入
* 不允许把所有逻辑写在一个文件中
* 每个核心函数要有 docstring
* 配置项统一写入 `config.py` 或 `.env`
* API Key 不得硬编码
* 所有路径使用相对路径或 `pathlib`

## 8.3 最低可运行命令

```bash
pip install -r requirements.txt
python src/build_kb.py
python src/ui.py
```

---

## 9. 数据与配置规范

## 9.1 环境变量

```env
DEEPSEEK_API_KEY=your_api_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com
EMBEDDING_MODEL=BAAI/bge-small-zh
VECTOR_DB_TYPE=chroma
VECTOR_DB_DIR=./vector_store
TOP_K=3
CHUNK_SIZE=400
CHUNK_OVERLAP=80
```

## 9.2 默认配置

| 配置项           | 默认值 | 说明      |
| ------------- | --: | ------- |
| CHUNK_SIZE    | 400 | 单块长度    |
| CHUNK_OVERLAP |  80 | 重叠长度    |
| TOP_K         |   3 | 默认召回数   |
| TEMPERATURE   | 0.2 | 降低幻觉    |
| MAX_DOCS      |   5 | 初始文档数建议 |

---

## 10. 验收标准

## 10.1 功能验收

| 验收项               | 必须达成 |
| ----------------- | ---- |
| 成功加载 3~5 篇文档      | 是    |
| 成功切分文本            | 是    |
| 成功生成 embedding    | 是    |
| 成功写入向量数据库         | 是    |
| 用户提问可触发检索         | 是    |
| 能输出 top-k 相关文本片段  | 是    |
| 能调用 DeepSeek 生成答案 | 是    |
| 答案附带来源说明          | 是    |
| 至少完成 3 个问题测试      | 是    |
| 支持 CLI 或 Web 界面   | 是    |

## 10.2 文档验收

必须提供：

* `requirements.txt`
* `README.md`
* 源代码
* 至少 3 组截图
* 实验报告 PDF

---

## 11. 风险与处理策略

| 风险       | 表现               | 处理策略                               |
| -------- | ---------------- | ---------------------------------- |
| 文档解析失败   | PDF/Word 内容为空或乱码 | 增加格式判断与异常处理                        |
| chunk 过大 | Prompt 超长、上下文溢出  | 控制 chunk size，减少 top-k             |
| chunk 过小 | 检索结果信息不足         | 增加 overlap，适当扩大 chunk              |
| 检索不准     | 命中无关内容           | 调整 embedding 模型、增大 top-k、引入 rerank |
| 幻觉输出     | 模型编造答案           | Prompt 中加入“无信息则拒答”约束               |
| API 成本高  | 调试阶段开销大          | 小样本测试，结果缓存                         |
| 本地运行慢    | embedding 或检索速度慢 | 先小规模建立知识库                          |

---

# 部署计划表（Deployment Plan）

## 12. 里程碑部署计划

| 阶段     | 任务编号 | 任务名称         | 具体工作                                      | 输入                    | 输出         | 负责人 | 预计耗时    | 完成标准        | 优先级 |
| ------ | ---- | ------------ | ----------------------------------------- | --------------------- | ---------- | --- | ------- | ----------- | --- |
| 第 1 阶段 | P1   | 初始化项目        | 创建目录结构、虚拟环境、安装依赖                          | 参考文档                  | 项目骨架       | 开发者 | 0.5 天   | 项目可运行       | 高   |
| 第 1 阶段 | P2   | 配置文件编写       | 编写 `requirements.txt`、`.env.example`、基础配置 | 依赖清单                  | 可复现配置      | 开发者 | 0.5 天   | 环境配置成功      | 高   |
| 第 2 阶段 | P3   | 文档加载模块       | 支持 txt/md/pdf/docx 的文本提取                  | 原始文档                  | 纯文本内容      | 开发者 | 1 天     | 3~5 篇文档可解析  | 高   |
| 第 2 阶段 | P4   | 文本切分模块       | 按 chunk size / overlap 切分文本               | 纯文本                   | chunk 列表   | 开发者 | 0.5 天   | chunk 结构正确  | 高   |
| 第 3 阶段 | P5   | Embedding 模块 | 加载 embedding 模型并向量化                       | chunk 列表              | embeddings | 开发者 | 0.5~1 天 | 向量生成成功      | 高   |
| 第 3 阶段 | P6   | 向量库模块        | Chroma/FAISS 建库与持久化                       | embeddings + metadata | 本地向量库      | 开发者 | 0.5 天   | 支持检索前加载     | 高   |
| 第 4 阶段 | P7   | 检索模块         | 用户问题向量化、top-k 检索                          | 用户问题                  | 检索结果       | 开发者 | 0.5 天   | 返回相关 chunk  | 高   |
| 第 4 阶段 | P8   | Prompt 构造模块  | 拼接参考内容与用户问题                               | 检索结果 + 问题             | 完整 Prompt  | 开发者 | 0.5 天   | Prompt 符合规范 | 高   |
| 第 5 阶段 | P9   | LLM 调用模块     | 接入 DeepSeek API/本地模型                      | Prompt                | 模型回答       | 开发者 | 0.5~1 天 | 回答生成成功      | 高   |
| 第 5 阶段 | P10  | RAG 主流程串联    | 把加载、检索、生成整合为 pipeline                     | 所有前置模块                | 可运行主流程     | 开发者 | 0.5 天   | 一次提问完整返回    | 高   |
| 第 6 阶段 | P11  | 对比实验模块       | 实现仅 LLM / 仅检索 / RAG 三模式                   | 问题集                   | 对比结果       | 开发者 | 0.5 天   | 至少 3 组实验完成  | 中   |
| 第 6 阶段 | P12  | CLI 界面       | 命令行问答交互                                   | pipeline              | 可交互问答      | 开发者 | 0.5 天   | 可连续提问       | 高   |
| 第 7 阶段 | P13  | Web 界面（可选）   | Gradio/Streamlit 展示结果                     | pipeline              | 网页 Demo    | 开发者 | 0.5~1 天 | 页面可访问       | 中   |
| 第 7 阶段 | P14  | README 编写    | 编写安装、运行、知识库说明                             | 项目代码                  | README.md  | 开发者 | 0.5 天   | 可独立复现       | 高   |
| 第 8 阶段 | P15  | 实验报告整理       | 结果截图、对比分析、问题总结                            | 运行结果                  | report.pdf | 开发者 | 1 天     | 满足作业提交要求    | 高   |
| 第 8 阶段 | P16  | 打包提交         | 整理 zip 提交内容                               | 全部成果                  | 最终压缩包      | 开发者 | 0.5 天   | 可直接提交       | 高   |

---

## 13. 建议执行顺序（可直接给 AI Agent）

```text
Step 1: 初始化项目目录和依赖
Step 2: 实现文档读取
Step 3: 实现文本切分
Step 4: 实现 embedding
Step 5: 建立向量数据库
Step 6: 实现检索
Step 7: 实现 DeepSeek 调用
Step 8: 串联 RAG 主流程
Step 9: 实现 CLI 界面
Step 10: 实现对比实验
Step 11: 可选实现 Web UI
Step 12: 编写 README 与报告素材
```

---

## 14. 交付物清单

| 交付物                  | 是否必须 | 说明          |
| -------------------- | ---- | ----------- |
| 源代码 `.py` / `.ipynb` | 是    | 主体实现        |
| `requirements.txt`   | 是    | 依赖说明        |
| `README.md`          | 是    | 环境、运行、说明    |
| 截图 3 组以上             | 是    | 包含提问、检索、回答  |
| 实验报告 PDF             | 是    | 原理、实验、问题、总结 |
| Web 界面               | 否    | 可加分         |
| 评估脚本 / 缓存 / 混合检索     | 否    | 可加分         |

---

## 15. 最终一句话执行要求

**请实现一个可复现、可运行、可展示、可提交的 DeepSeek RAG 课程项目，满足“文档处理 + 向量化 + 检索 + 生成 + 对比实验 + 基础交互 + 文档交付”的完整闭环。**

---

如果你需要，我可以下一步直接把这份内容继续整理成：

1. **更严格的 PRD 版本**，或者
2. **可直接用于 Cursor / Copilot / ChatGPT Coding Agent 的任务拆解清单**。
